# Speech recognition - Hybrid Architecture
# Vosk: Wake word detection (local, offline)
vosk==0.3.45
# SpeechRecognition: Google Speech API for commands (cloud, accurate, free)
SpeechRecognition>=3.10.0

# Audio capture
pyaudio==0.2.14

# Voice Activity Detection (reduces CPU usage)
webrtcvad==2.0.10

# Tidal API integration
tidalapi>=0.8.10

# Chromecast protocol
pychromecast==14.0.1

# Environment variables
python-dotenv==1.0.0

# Utilities
requests>=2.32.3

# Phonetic matching
g2p-en==2.1.0
fuzzywuzzy==0.18.0
nltk==3.8.1
# For better performance, install python-Levenshtein
python-Levenshtein

# Text-to-speech for testing (optional)
gtts==2.5.0

# Local TTS (offline, faster than Google TTS)
pyttsx3==2.90

# Testing
pytest>=7.0.0
